# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PandemicVisualiser' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PandemicVisualiser

  target 'PandemicVisualiserTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PandemicVisualiserUITests' do
    # Pods for testing
  end
pod 'Alamofire', '~> 5.0'
pod 'SwiftyJSON', '~> 4.0'

end
